// Re-export from canonical source; kept for backwards compat with any existing imports
export type { ReservationStatus, AdminReserva as Reservation, ReservaFilterKey } from '../../data/types';
export { STATUS_STYLES as statusStyles, FILTER_LABELS } from '../../data/types';
